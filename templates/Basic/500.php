<!DOCTYPE html>
<html lang="{{langCode}}" dir="ltr">
    <head>
        <base href="{{baseHref}}" />
        <meta charset="utf-8" />
        <meta content="width=device-width, initial-scale=1.0" name="viewport" />
        <title>{{title}}</title>
        {{styles}}
    </head>
    <body class="page500">
        <div class="wrapper">
            <div class="box">
                <h3 class="animated fadeInDown">Internal server error</h3>
                <div class="icon animated fadeInUp"></div>
            </div>
        </div>
        {{content}}
        <div id="messageArea"></div>
        {{scripts}}
    </body>
</html>
